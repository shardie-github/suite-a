/* CONTRIBUTING.md — © Hardonia. MIT. */
# Contributing
- Conventional Commits (feat|fix|chore|docs|refactor|test|build|ci).
- Run: `npm run lint && npm run format && npm run qa:repo` before PRs.
- Update docs in `docs/` with any feature changes.
