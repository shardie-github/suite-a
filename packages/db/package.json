{
  "devDependencies": {
    "prisma": "5.20.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@prisma/client": "5.20.0"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "scripts": {
    "generate": "prisma generate --schema prisma/schema.prisma",
    "build": "tsc -b",
    "dev": "tsc -w"
  }
}